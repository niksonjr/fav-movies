# Favourite Movies Test App

### Prerequisites
    - git
    - node.js
    - npm
    - gulp
    - react
    - redux


## Installation on local machine
    - `git clone [repo]`
    - npm install
    - `gulp watch` for development watch mode or `gulp build --env=development` for development build
